[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "biblioteca-protheus"
version = "0.1.0"
description = "Biblioteca de funções para automações Protheus"
requires-python = ">=3.10"